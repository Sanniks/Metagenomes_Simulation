# Documentation

From this folder, build the documentation with:

```bash
./make html
```

To build the PDF version through LaTeX:

```bash
./make latexpdf
```

The generated files are saved in `build/`.
